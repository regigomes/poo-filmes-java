// Ator herda da classe abstrata Pessoa
// A classe Ator não precisa de atributos adicionais, pois herda de Pessoa
public class Ator extends Pessoa { // // A palavra 'extends' indica que Ator herda de Pessoa 
    public Ator(String nome) {
        super(nome);
    }
}
// Essa classe não precisa de atributos, pois já herda de Pessoa
    // Mas você pode adicionar se quiser